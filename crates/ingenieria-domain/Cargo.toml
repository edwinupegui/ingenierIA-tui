[package]
name        = "ingenieria-domain"
version.workspace = true
edition.workspace = true
description = "Domain types for ingenierIA TUI — zero internal dependencies"

[dependencies]
serde      = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
serde_json = "1"

[package]
name        = "ingenieria-api"
version.workspace = true
edition.workspace = true
description = "Chat API types, pricing, retry, metrics, and stream parsing for ingenierIA TUI"

[dependencies]
serde_json  = "1"
futures-util = "0.3"
